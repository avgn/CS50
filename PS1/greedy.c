#include <stdio.h>

int main(void)
{
    float change;
    int ncoin;

    do
    {    
        printf("O hai! How much change is owed?\n");
        scanf("%f", &change);
    }
    while (change < 0);

    while (change


}
